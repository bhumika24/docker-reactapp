---
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - me-central1-docker.pkg.dev/ardent-iris-389305/gcp-azure-pipeline/myimage
      - .
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - me-central1-docker.pkg.dev/ardent-iris-389305/gcp-azure-pipeline/myimage
artifacts:
  objects:
    location: gs://codebuild/build-logs/
    paths:
      - /var/log/cloud-build.log

